import {useParams, useHistory} from 'react-router-dom';
import
import {getMealById} from '../api';

function Recipe(){
    const {id} = useParams();

    гіу
  return (
      <>

      </>
  )
}

export {Recipe}